import Piece


class Pawn(Piece):
    def __init__(self, color, location, board):
        super().__init__(color, location, board)

    def setMoves(self):
        # White Pieces
        if self.color == 1:
            tempMoves = []
            if
            self.setMoves()
        # Black Pieces
        else:
